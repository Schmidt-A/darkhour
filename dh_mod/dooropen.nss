void main()
{

object oPC = GetLastOpenedBy();
/*
if (!GetIsPC(oPC)) return;

//object oDeny;
if (GetXP(oPC)!= 0)// OBJECT_INVALID)
   {
   //oDeny = GetWaypointByTag("kalaramdarkalley");

   //AssignCommand(oPC, JumpToObject(oDeny));

   return;
   }

object oTarget;
oTarget = GetObjectByTag("StartingDoor");

AssignCommand(oTarget, ActionCloseDoor(oTarget));
*/

}
