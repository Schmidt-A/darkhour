//#include "aps_include"

// Modifed by Zunath on July 21, 2007 to include APS/NWNX2

void main()
{
    //if(GetCampaignInt("NWNX2_Persistance","NWNX2_Locations")==1)
    //{EndGame("End_Movie");}
    //int nNumber = 0;
    //object oRandom = GetObjectByTag("Randomizer",nNumber);
    //while (oRandom != OBJECT_INVALID)
    //{
    //    DelayCommand(30.0,ExecuteScript("randomscript",oRandom));
    //    nNumber += 1;
    //    oRandom = GetObjectByTag("Randomizer",nNumber);
    //}

}
